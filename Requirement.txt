# === Core Environment ===
python-dotenv==1.0.1
streamlit==1.39.0
pandas==2.2.3
numpy==2.1.2

# === Google APIs ===
google-api-python-client==2.149.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
oauth2client==4.1.3
gspread==6.1.2

# === AI + LangChain + LangGraph ===
langchain-google-genai==1.0.7
langgraph==0.2.28
langchain-core==0.3.14
langchain==0.3.7

# === Email Utilities ===
email-validator==2.2.0

# === Frontend Enhancements ===
st-aggrid==1.0.5

# === Misc Tools ===
python-dateutil==2.9.0.post0
requests==2.32.3
protobuf==5.28.2

---------------------------------------------------
 
# Step 1: Create a new environment with Python 3.13.5
conda create -n faraz python ==3.13.5

# Step 2: Activate the environment

conda activate faraz

# Step 3: Upgrade pip
pip install --upgrade pip

--------------------------------------------------

First of all run the authorize_google.py file through python authorize_google.py
then a  link will appear in the terminal and then open the link in google and verify the account 

then  once the verification is completed run app.py file 

then you are ready to go .....


